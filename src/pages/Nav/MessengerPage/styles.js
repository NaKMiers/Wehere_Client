import { makeStyles } from '@material-ui/core'

export default makeStyles(theme => ({
   noCvsWrap: {
      textAlign: 'center',
      marginTop: 24,
   },
   noCvs: {
      fontSize: 36,
      color: theme.border.light,
   },
}))
