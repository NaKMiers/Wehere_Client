import { makeStyles } from '@material-ui/core'

export default makeStyles(theme => ({
   noFriendOnline: {
      textAlign: 'center',
      marginTop: 20,
      fontSize: 48,
      color: theme.border.light,
   },
}))
