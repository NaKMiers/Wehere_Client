import { makeStyles } from '@material-ui/core'

export default makeStyles(theme => ({
   link: {
      display: 'flex',
      textDecoration: 'none',
      width: '100%',
      color: '#333'
   },
   recentlyList: {
      marginLeft: 24
   }
}))
